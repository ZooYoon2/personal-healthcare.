<?xml version="1.0" encoding="UTF-8"?>
 
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- cate.xml과 연동되는 Spring interface "dev.mvc.cate.ExertypeDAOInter" 선언 --> 
<mapper namespace="dev.mvc.exertype.ExertypeDAOInter">
  <insert id="create" parameterType="dev.mvc.exertype.ExertypeVO">
    INSERT INTO EXERTYPE(EXERCISE_TYPE_NO, EXERCISE_TYPE_NAME, EXERCISE_TYPE_CNT)
    VALUES (EXERCISE_TYPE_SEQ.NEXTVAL, #{EXERCISE_TYPE_NAME}, 0)
  </insert> 
  
  <!-- 
  전체 목록, 출력 순서 반영
  레코드 갯수 만큼 ExertypeVO 객체를 만들어 ArrayList에 저장하여 리턴,
  ArrayList<ExertypeVO> list = new ArrayList<ExertypeVO>(); 
  -->
  <select id="list_all" resultType="dev.mvc.exertype.ExertypeVO">
		SELECT EXERCISE_TYPE_NO, EXERCISE_TYPE_NAME, EXERCISE_TYPE_CNT
		FROM EXERTYPE
		ORDER BY EXERCISE_TYPE_NO ASC
  </select>
  
  <!-- 조회, id: read, 입력: cateno, 리턴: ExertypeVO -->
  <select id="read" resultType="dev.mvc.exertype.ExertypeVO" parameterType="int">
		SELECT EXERCISE_TYPE_NO, EXERCISE_TYPE_NAME, EXERCISE_TYPE_CNT
		FROM EXERTYPE
		WHERE EXERCISE_TYPE_NO = #{EXERCISE_TYPE_NO}
  </select>  

  <!-- 수정 처리 -->  
  <update id="update" parameterType="dev.mvc.exertype.ExertypeVO">
		UPDATE EXERTYPE
		SET EXERCISE_TYPE_NO=#{EXERCISE_TYPE_NO}, EXERCISE_TYPE_NAME=#{EXERCISE_TYPE_NAME}
		WHERE EXERCISE_TYPE_NO=#{EXERCISE_TYPE_NO}
  </update>  
  
  <!-- 삭제, return: int -->
  <delete id="delete" parameterType="int">
    DELETE FROM EXERTYPE
    WHERE EXERCISE_TYPE_NO = #{EXERCISE_TYPE_NO}
  </delete>      
</mapper>




