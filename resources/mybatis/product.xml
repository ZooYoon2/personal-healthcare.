<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- contents.xml과 연동되는 Spring interface "dev.mvc.contents.ContentsDAOInter" 선언 --> 
<mapper namespace="dev.mvc.product.ProductDAOInter">

  <!-- 운동기구 등록  -->
  <insert id="create" parameterType="dev.mvc.product.ProductVO">
    INSERT INTO product(productId, product, genre, file1, file1saved, thumb1, size1)
    VALUES(product_seq.nextval, #{productId}, #{product}, #{genre}, #{file1}, #{file1saved}, #{thumb1}, #{size1});
  </insert>
  
  <!-- 운동기구 추천 목록, WHERE productId IN (28, 16, 29, 6, 14) -->
  <select id="mf_product_member" resultType="dev.mvc.product.ProductVO" parameterType="HashMap">
		SELECT productId, product, genre, file1, file1saved, thumb1, size1
		FROM product
		WHERE productId IN <foreach collection="productno_list" item="item" index="index" open="(" separator="," close=")">#{item}</foreach>
		ORDER BY productId  ASC
  </select>
  
            
</mapper>





